services:
  - type: web
    name: lllm-analysis-quiz-solver
    env: docker
    repo: https://github.com/JeevanNairPK/LLMQUIZ
    plan: starter
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
